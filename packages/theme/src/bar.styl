.l-bar
    border-radius 15px
    background-color $lw1 + 20%
    cursor pointer
    transition opacity 120ms ease-out

    &.is-candy
        box-sizing content-box
        border 2px dashed $lg2
        background-color $lo2
        overflow visible