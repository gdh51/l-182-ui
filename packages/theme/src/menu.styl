@import '../shared.styl'

.l-menu
    display flex
    width 100%
    comPad()

    &.l-menu_left
        justify-content flex-start

    &.l-menu_right
        justify-content flex-end

    &.l-menu_center
        justify-content center