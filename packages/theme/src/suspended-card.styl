.l-suspended-card
    background-color #fff
    cursor pointer
    transition transform .25s ease-out, box-shadow .25s ease-out
    user-select none

    &:hover
        box-shadow 0 24px 48px 10px rgba(35, 51, 64, .35)
        transform translateY(-2px)